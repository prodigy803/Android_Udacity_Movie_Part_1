<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- Settings, should always be in the overflow -->
    <item android:id="@+id/action_top_rated"
        android:title="@string/top_rated_movies"
        app:showAsAction="never"/>

    <item android:id="@+id/action_now_playing_movies"
        android:title="@string/now_playing_movies"
        app:showAsAction="never"/>

    <item android:id="@+id/action_upcoming_movies"
        android:title="@string/upcoming_movies"
        app:showAsAction="never"/>

    <item android:id="@+id/action_popular_movies"
        android:title="@string/popular_movies"
        app:showAsAction="never"/>


</menu>